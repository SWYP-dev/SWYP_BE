server:
  port: 8080

app:
  # TODO: 인증 기능 머지 후 제거. 그 전까지 모든 요청을 이 유저로 처리한다.
  test-user-id: 1

spring:
  application:
    name: server
  jpa:
    hibernate:
      ddl-auto: validate

logging:
  level:
    root: info
    com.chwihap.server: debug
