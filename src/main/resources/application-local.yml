spring:
  datasource:
    url: jdbc:mysql://localhost:3306/chwihap
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379

app:
  jwt:
    secret: local-dev-jwt-secret-key-please-change-this-1234567890
  kakao:
    client-id: local-dev-kakao-client-id
    redirect-uri: http://localhost:3000/oauth/kakao/callback
